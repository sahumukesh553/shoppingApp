import { AternateIfDirective } from './aternate-if.directive';

describe('AternateIfDirective', () => {
  it('should create an instance', () => {
    const directive = new AternateIfDirective();
    expect(directive).toBeTruthy();
  });
});
